#pragma once
#include <random>


// generates random floating point value in specified range
extern float randValFloat(float minVal, float maxVal);


// generates random integer value in specified range
extern int randValInt(int minVal, int maxVal);